# CSE341

The RESTful API are: 

GET /contact/:contactId   => retrieve a single contact by id
GET /contacts             => retrieve all the contacts 
POST /contact             => create a new contact 
